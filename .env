DB_HOST=localhost # Usually localhost, but try 127.0.0.1 if it fails
DB_NAME=root
DB_USER=root # Must match the user you created in cPanel MySQL
DB_PASS=your_password # Must match the password you set in cPanel MySQL

OPENNODE_API_KEY=2975300e-29xxxxxxxxxxxxxxxxxxxxxxfe
OPENNODE_ENVIRONMENT=live # dev or live

APP_URL=http://localhost:8000
APP_NAME="Lightning Pay"
ADMIN_USER=admin
ADMIN_PASS=admin123
